
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python_dev_container
    volumes:
      # Mount the current project directory into /app inside the container
      - ./:/app
    # Keep the container running without starting a specific application
    command: tail -f /dev/null
